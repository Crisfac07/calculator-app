services:
  backend:
    build: ./backend
    expose:
      - "8080"

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    environment:
      BACKEND_URL: http://backend:8080
    depends_on:
      - backend